.db-context/
.scratch/reports/
.superpowers/
*.db-context.json
*.db-context.md
*.db-context.config.json
db-context.config.json
.db-context.config.json
.env.db-context
.env
.env.*
!.env.example
routines.sql
jobs.md
# Harness runtime artifacts: .bak backups, audit log, lockfile, install
# manifest — never enter version control (T0-1 CC2 / ADR Ledger L18).
.harness/

# Smoke/test fixture scratch — accumulates Python bytecode cache, can grow to GB.
tmp/

# Claude Code worktrees — content lives in linked branches (already merged to develop).
.claude/

# v0.9.4 test fixture tarballs — always rebuilt by scripts/build_v094_fixture.py;
# only the .sha256 pin files are checked-in (CI verifies the hash after rebuild).
tests/fixtures/*.tar.gz

# Python build / install artifacts
__pycache__/
*.egg-info/
build/
