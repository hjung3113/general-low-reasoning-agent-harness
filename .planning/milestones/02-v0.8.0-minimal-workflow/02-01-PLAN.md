# Milestone 02 — v0.8.0 Minimal Workflow Plan

Status: **done**.

## Goal

Strip unused autopilot/budget/halt-diary scaffolding + orphan migration code paths. Land minimal manual-mode workflow as canonical.

## Items

1. Remove `state_migrate` v0↔v2 (695 LOC src + 1126 LOC tests) — `e61fe22`.
2. Remove `verify_adrs_accepted.py` — `ce62ed9`.
3. Remove `scripts/smoke/` live-trial harness (~3,170 LOC) — `eedef26`.
4. Remove `smoke_lifecycle` driver (1,156 LOC orphan) — `ffddf34`.
5. Slim `audit_chain.py` — drop verify/walk path — `752ad68`.
6. (Skipped.)
7. Full purge of autopilot/budget/halt-diary scaffolding (~2,000 LOC) — `8d23817`.
8. Infra hygiene — fix stale pyproject + drop broken CI PS deny-fuzz — `92679f6`.

Final docs sync: `eeab5b4`.

## Verification

- `python3 -m unittest scripts.tests.test_harness` (note: 44% failure rate post-strip; tracked separately).
- `python3 scripts/harness.py check`
- `python3 scripts/harness.py check --worktree`
