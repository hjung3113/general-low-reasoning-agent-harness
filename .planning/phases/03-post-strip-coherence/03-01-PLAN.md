# Milestone 03 — Post-strip coherence

Close the consistency gap left by Milestones 1 + 2. Two stripping milestones removed ~3,930 + ~2,000 LOC; some tests, schema fields, and template strings still reference the removed concepts, and `develop` carries 7 unrelated test failures. Bring the codebase, tests, and templates into agreement with the new contract (CONTEXT.md + ADRs 0001-0003).

Co-grilled with codex 2026-05-23. Both agents agreed on item ordering. No unresolved disagreements.

## Constraints (CONTEXT.md + ADRs)

- Internal tool, no external attacker (ADR-0002).
- Execution always manual (ADR-0001).
- State schema v2 only (ADR-0003).
- Workflow enforces ~70%, judgment/quality not enforced.
- No new modules or tests. **Strip and align only.**

## Items

### M3-1. Failing-test repair (rank-1, broken-develop blocker)

7 pre-existing test failures on `develop` (independent of this session's commits):

- 5 in `tests/test_error_code_consistency.py` — assert `docs/error-code-map.md` exists with required codes and table rows. File was stripped or never created. **Decide per finding**: restore canonical artifact, fix tests to match new reality, or delete obsolete tests.
- 1 in `tests/phase_state/test_state_trust_preflight.py::test_preflight_rejects_empty_state_file_distinctly` — asserts `"recover"` substring in `StateEmptyError` message; actual message uses `"restore"`. Fix test or fix message.
- 1 in `tests/phase_reopen/test_reopen.py::test_email_not_in_approvers_rejected` — investigate.

Exit criterion: full `pytest tests/` is 0 fail (real-file fixture tests excluded by long-standing convention).

### M3-2. trust_origin residue strip (rank-2, ADR-0002 contradiction)

`scripts/lib/upgrade.py` carries 3 residual `trust_origin` references at lines 181/204/276 plus state-field writes for `release_tag` / `release_commit`. ADR-0002 says no external attacker → trust-origin tracking is dead code that contradicts the standing decision.

Strip:
- `upgrade.py` field writes + the `"dev_unsigned"` literal
- `manifest_v2.py` schema entries for these fields (if present)
- any tests asserting their presence
- docs/INSTALL-MODEL.md mentions of `trust_origin` semantics

Exit criterion: `grep -rn "trust_origin\|release_tag\|release_commit" scripts/ tests/` returns 0 hits, install/upgrade tests pass.

### M3-3. Profile audit (rank-3)

`scripts/lib/profiles.py` defines 4 profiles (`generic`, `dotnet-etl`, `python-etl`, `react-web`) plus `_PROFILE_DEFAULT_PACKS` and `_DB_PACKS`. Verify each profile is actually selected in a real install path / target. Cull any unused profile + its skill-pack defaults. **Evidence required** — do not delete without grep'ing for selection sites and any external usage.

### M3-4. target_smoke_test.py usage check

`scripts/target_smoke_test.py` (95 LOC) — confirm whether anything still invokes this (CI, full_smoke.sh, manual usage). If dead, delete. If load-bearing but stale, document.

### M3-5. Manifest + skeleton sweep for stripped-concept residue (codex catch)

Sweep `harness/manifest.json` + `harness/skeleton/clean/**` for textual references to removed concepts:
- autopilot / autopilot_run / phase_autopilot
- trust_origin / release_trust / SSH signing
- v0/v1 state migration
- halt-diary / cli_budgets / EXIT_NO_ACTION_DURING_AUTOPILOT (exit 18)

For each hit: delete if dead, update if still load-bearing for target repos.

## Execution order

M3-1 first (broken develop blocks signal). M3-2 next (ADR contradiction). M3-3/M3-4/M3-5 parallel-safe; sequence opportunistically.

## Definition of done

- `pytest tests/` 0 fail (real-file fixtures excluded).
- `grep -rn "trust_origin"` returns 0 in source + tests.
- All 4 profiles either confirmed live or removed with evidence.
- `target_smoke_test.py` confirmed live or removed.
- Manifest + skeleton scan returns 0 references to ADR-removed concepts.
- ROADMAP Milestone 3 bullet checked off; STATE.md transitioned to next milestone or to `done`.
