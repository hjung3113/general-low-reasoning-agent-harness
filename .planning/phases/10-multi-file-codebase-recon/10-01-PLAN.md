# Milestone 10 — multi-file .planning/codebase/ + skill-pack integration

Restore 8-file `.planning/codebase/` structure lost in nuke 3048847. Lock anchor-ID schema (ADR-0008). Wire workflow skill-packs to consume anchors via `reads:` frontmatter contract.

## Deliverables

- ADR-0008: multi-file codebase recon schema
- `docs/CODEBASE-SCHEMA.md`: 43-anchor reference + frontmatter spec
- 8 skeleton stubs in `harness/skeleton/clean/.planning/codebase/`
- Manifest: 8 seed entries + `codebase-recon.md` retired v0.10.0
- `scripts/lib/recon.py` rewritten: `build_codebase_docs()` multi-file output
- 4 workflow skill-pack SKILL.md frontmatter: `reads:` anchor declarations
- 25 unit/integration tests
- README + MANUAL (en+ko) updated

## Codex review

R1 + R2 verdicts all ≥80 except migration (78); user picked clean replace. See PR #35 description.

## Verification

- 25 tests pass
- Smoke test on synthetic Python+Node+pg target: 8 files, anchors present, frontmatter stamped, idempotent dry-run, agent body preservation
- Haiku live test: anchor grep across multiple files returns correct facts

## Status

COMPLETE. PR #35 merged 2026-05-23.
