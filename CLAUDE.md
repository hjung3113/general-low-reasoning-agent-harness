# CLAUDE.md

Project-level instructions for Claude Code in this repo.

## Agent skills

### Issue tracker

Issues live as GitHub issues at `hjung3113/general-low-reasoning-agent-harness`; use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) — defaults; all created on the GitHub repo. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root, alongside durable phase memory under `.planning/**`. See `docs/agents/domain.md`.
